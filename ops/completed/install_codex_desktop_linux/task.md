# Install Codex Desktop Linux

## 2026-06-26 21:03:13 — Codex
Task: install this checkout of `codex-desktop-linux` using the documented native package flow on Ubuntu 24.04.

Planned command:

```bash
make bootstrap-native
```

Expected receipt:

- Installed Debian package metadata for Codex Desktop.
- Installed launcher or update-manager command available on the live system.
- systemd user updater status checked when the service is installed.

